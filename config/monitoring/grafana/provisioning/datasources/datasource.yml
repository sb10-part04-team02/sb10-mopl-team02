# Grafana 기동 시 Prometheus 데이터소스를 자동 등록한다.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus   # 대시보드 JSON에서 이 uid로 참조한다
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
